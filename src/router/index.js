import Vue from 'vue'
import Router from 'vue-router'

Vue.use(Router)

// get components

// initialize router with the respective routes
export default new Router({
	mode: 'history',
	routes: [

	]
})
